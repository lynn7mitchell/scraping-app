// use app.post to post article to database when saved is clicked
    // use app.get to get articles when on the /saved page
    // use materialize to create modal for when the "comment" btn is clicked
    // when the "submit" btn is clicked the comment will use app.post to send it to the database
    // comments will show will be added in the comments section when the "submit" btn is clicked using jquery
    // add db.Article.deleteMany({}).catch(function (err) {console.log(err)}); to every route nested in the "scrape" btn